# Copy to `.env` in this folder (`supabase/.env`). Used by `supabase start` for local Auth.

# Local Google OAuth (set [auth.external.google] enabled = true in config.toml)
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
